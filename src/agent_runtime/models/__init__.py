"""Models package placeholder for future Pydantic-based schemas."""

from pydantic import BaseModel


class PlaceholderModel(BaseModel):
    """Minimal placeholder model for Phase 0 import validation."""

    name: str = "placeholder"
